#include <stdio.h>

int main() {
    int n = 0;
    int i ;
    printf( "กรุณาป้อนจำนวน n: " );
    scanf( "%d", &n );

    for ( i = 1 ; i <= n ; i++)  {
        printf( "%d Hello World\n" , i );
    }

    return 0;
}
